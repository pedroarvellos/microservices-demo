spring:
  application:
    name: configserver
  profiles:
    active: composite
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pedroarvellos/config-server-repository.git
          clone-on-start: true
        bootstrap: true
    fail-fast: true
